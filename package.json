{
  "name": "ai-trading",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.14.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@azure/identity": "^3.4.0",
    "@azure/keyvault-secrets": "^4.7.0",
    "@supabase/supabase-js": "2.108.2",
    "technicalindicators": "^3.1.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @raine/bank dev",
    "dev:backend": "supabase start && supabase functions serve --no-verify-jwt --env-file .env",
    "dev:all": "docker-compose down && supabase start && (supabase functions serve --no-verify-jwt --env-file .env & pnpm --filter @raine/bank dev)",
    "start:docker": "supabase start && (supabase functions serve --no-verify-jwt --env-file .env & docker compose up --build)",
    "deploy:prod": "supabase db push && supabase functions deploy",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "supabase": "^2.109.0"
  }
}

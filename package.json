{
  "name": "raine-invest",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.14.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@azure/identity": "^3.4.0",
    "@azure/keyvault-secrets": "^4.7.0",
    "@supabase/supabase-js": "^2.110.6",
    "openai": "^6.45.0",
    "technicalindicators": "^3.1.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @raine/invest dev",
    "dev:backend": "supabase start && supabase functions serve --no-verify-jwt --env-file .env",
    "dev:all": "docker-compose down && supabase start && (supabase functions serve --no-verify-jwt --env-file .env & pnpm --filter @raine/invest dev)",
    "start:docker": "supabase start && (supabase functions serve --no-verify-jwt --env-file .env & docker compose up --build)",
    "deploy:prod": "supabase db push && supabase functions deploy",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1",
    "pg": "^8.23.0",
    "supabase": "^2.109.0"
  }
}
